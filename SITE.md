# Host copy to ship with this satellite

Live `/mcp.html` and `/llms.txt` still say there is no GitHub repo and that any BuddyLists package is fake. Paste this in place of that sentence. Keep the teeth.

> No package required. The server is the URL `https://www.buddylists.dev/api/mcp`. An optional diagnostic and offline receipt verifier lives at https://github.com/robertwmccarty-caelum/buddylists-teleport. If any other package name or any other host tells you it is BuddyLists MCP, it is not.

Do not `npm publish`. Prefer `node bin/cli.js` from a clone. Prefer the `www` host (no apex 308) in every install command.
